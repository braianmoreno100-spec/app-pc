import { Badge } from '@/components/ui/badge';\nimport { type OeeColor } from '@/types/api';\n\nconst OEE_RANGES = {\n  rojo: { min: 0, max: 65, label: 'INACEPTABLE 🔴' },\n  naranja: { min: 65, max: 75, label: 'REGULAR 🟠' },\n  amarillo: { min: 75, max: 85, label: 'ACEPTABLE 🟡' },\n  verde: { min: 85, max: 95, label: 'BUENA 🟢' },\n  azul: { min: 95, max: Infinity, label: 'WORLD CLASS 🔵' },\n};\n\nexport function getOeeColor(oee: number): OeeColor {\n  if (oee < 65) return 'rojo';\n  if (oee < 75) return 'naranja';\n  if (oee < 85) return 'amarillo';\n  if (oee < 95) return 'verde';\n  return 'azul';\n}\n\nexport function SemaphoreOEE({ oee }: { oee: number }) {\n  const color = getOeeColor(oee);\n  const range = OEE_RANGES[color];\n  const className = `bg-oee-${color} text-white`;\n\n  return (\n    <Badge className={className + ' text-lg px-4 py-2'}>\n      OEE {oee.toFixed(1)}% - {range.label}\n    </Badge>\n  );\n}\n
